{{- if .Values.configMap.create }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mimir-envoy.fullname" . }}-config
  labels:
    {{- include "mimir-envoy.labels" . | nindent 4 }}
data:
  envoy.yaml: |
    admin:
      address:
        socket_address:
          address: 0.0.0.0
          port_value: 9901
      access_log:
        - name: envoy.access_loggers.stdout
          typed_config:
            "@type": type.googleapis.com/envoy.extensions.access_loggers.stream.v3.StdoutAccessLog
            log_format:
              text_format: "[%START_TIME%] %REQ(:METHOD)% %REQ(X-ENVOY-ORIGINAL-PATH?:PATH)% %PROTOCOL% %RESPONSE_CODE% %RESPONSE_FLAGS% %BYTES_SENT% %DURATION% %REQ(X-FORWARDED-FOR)% %REQ(USER-AGENT)% %REQ(X-REQUEST-ID)% %REQ(:AUTHORITY)% %UPSTREAM_HOST%\n"

    # Overload manager with memory monitoring (compatible with all Envoy versions)
    overload_manager:
      refresh_interval: 0.25s
      resource_monitors:
      - name: "envoy.resource_monitors.fixed_heap"
        typed_config:
          "@type": type.googleapis.com/envoy.extensions.resource_monitors.fixed_heap.v3.FixedHeapConfig
          max_heap_size_bytes: {{ .Values.resourceLimits.maxHeapSizeBytes | int }}
      actions:
      - name: "envoy.overload_actions.stop_accepting_requests"
        triggers:
        - name: "envoy.resource_monitors.fixed_heap"
          threshold:
            value: {{ .Values.resourceLimits.heapStopAcceptingThreshold }}
      - name: "envoy.overload_actions.shrink_heap"
        triggers:
        - name: "envoy.resource_monitors.fixed_heap"
          threshold:
            value: {{ .Values.resourceLimits.shrinkHeapThreshold }}

    static_resources:
      listeners:
      - name: listener_0
        address:
          socket_address:
            address: 0.0.0.0
            port_value: {{ .Values.service.port | int }}
        filter_chains:
        - filters:
          - name: envoy.filters.network.http_connection_manager
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
              stat_prefix: ingress_http
              codec_type: AUTO
              # 🔧 FIX: Accept HTTP/1.1 from NGINX (prevents 426 Upgrade Required errors)
              http_protocol_options:
                accept_http_10: {{ .Values.proxy.httpProtocol.acceptHttp10 }}
                default_host_for_http_10: {{ include "mimir-envoy.fullname" . | quote }}
              # 🔧 FIX: Add timeouts to prevent hanging requests and buffer issues
              request_timeout: {{ .Values.extAuthz.timeout | default "10s" }}
              stream_idle_timeout: 300s
              # Enable proper handling of forwarded headers from NGINX
              use_remote_address: {{ .Values.proxy.httpProtocol.useRemoteAddress }}
              xff_num_trusted_hops: {{ .Values.proxy.httpProtocol.xffNumTrustedHops | int }}
              {{- if .Values.logging.enableAccessLogs }}
              # 🔧 DEBUG: Enable access logs for troubleshooting
              access_log:
              - name: envoy.access_loggers.stdout
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.access_loggers.stream.v3.StdoutAccessLog
                  log_format:
                    text_format: |
                      [%START_TIME%] "%REQ(:METHOD)% %REQ(X-ENVOY-ORIGINAL-PATH?:PATH)% %PROTOCOL%"
                      %RESPONSE_CODE% %RESPONSE_FLAGS% %BYTES_RECEIVED% %BYTES_SENT%
                      %DURATION% %RESP(X-ENVOY-UPSTREAM-SERVICE-TIME)% "%REQ(X-FORWARDED-FOR)%"
                      "%REQ(USER-AGENT)%" "%REQ(X-REQUEST-ID)%" "%REQ(:AUTHORITY)%" "%UPSTREAM_HOST%"
                      tenant="%REQ({{ .Values.tenantHeader }})%"
              {{- end }}
              route_config:
                name: local_route
                virtual_hosts:
                - name: local_service
                  domains: ["*"]
                  routes:
                  - match:
                      prefix: "{{ .Values.remoteWrite.path }}"
                    route:
                      cluster: mimir_distributor
                      timeout: {{ .Values.proxy.upstreamTimeout }}
                  # 🔧 DEBUG: Catch-all route for debugging (returns 404 for unmatched paths)
                  - match:
                      prefix: "/"
                    direct_response:
                      status: 404
                      body:
                        inline_string: "Path not found. Expected: {{ .Values.remoteWrite.path }}"
              http_filters:
              - name: envoy.filters.http.ext_authz
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.ext_authz.v3.ExtAuthz
                  transport_api_version: V3
                  with_request_body:
                    max_request_bytes: {{ .Values.extAuthz.maxRequestBytes | int }}
                    allow_partial_message: true
                  failure_mode_allow: {{ .Values.extAuthz.failureModeAllow }}
                  grpc_service:
                    envoy_grpc:
                      cluster_name: rls_ext_authz
                    # 🔧 FIX: Add timeout to prevent hanging requests
                    timeout: {{ .Values.extAuthz.timeout | default "5s" }}
              - name: envoy.filters.http.ratelimit
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.ratelimit.v3.RateLimit
                  domain: {{ .Values.rateLimit.domain }}
                  rate_limit_service:
                    transport_api_version: V3
                    grpc_service:
                      envoy_grpc:
                        cluster_name: rls_ratelimit
                      # 🔧 FIX: Add gRPC timeout for rate limit
                      timeout: {{ .Values.rateLimit.grpcTimeout | default "2s" }}
                  failure_mode_deny: {{ .Values.rateLimit.failureModeDeny }}
              - name: envoy.filters.http.router
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
                  dynamic_stats: true

      clusters:
      - name: mimir_distributor
        connect_timeout: 0.25s
        type: STRICT_DNS
        lb_policy: ROUND_ROBIN
        # 🔧 FIX: Use HTTP/1.1 for Mimir distributor (most Mimir deployments use HTTP/1.1)
        typed_extension_protocol_options:
          envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
            "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
            explicit_http_config:
              http_protocol_options: {}  # HTTP/1.1
        load_assignment:
          cluster_name: mimir_distributor
          endpoints:
          - lb_endpoints:
            - endpoint:
                address:
                  socket_address:
                    address: {{ .Values.mimir.distributorHost }}
                    port_value: {{ .Values.mimir.distributorPort | int }}

      - name: rls_ext_authz
        connect_timeout: 0.25s
        type: STRICT_DNS
        lb_policy: ROUND_ROBIN
        # 🔧 FIX: Properly configure HTTP/2 for gRPC communication to RLS
        typed_extension_protocol_options:
          envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
            "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
            explicit_http_config:
              http2_protocol_options: 
                # 🔧 FIX: Configure HTTP/2 settings for better gRPC performance
                max_concurrent_streams: 100
        # 🔧 FIX: Add health checks for RLS service
        health_checks:
        - timeout: 1s
          interval: 5s
          unhealthy_threshold: 2
          healthy_threshold: 1
          http_health_check:
            path: "/health"
        # 🔧 FIX: Configure connection pool for better reliability
        circuit_breakers:
          thresholds:
          - priority: DEFAULT
            max_connections: 100
            max_pending_requests: 50
            max_requests: 200
            max_retries: 3
        load_assignment:
          cluster_name: rls_ext_authz
          endpoints:
          - lb_endpoints:
            - endpoint:
                address:
                  socket_address:
                    address: {{ .Values.rls.host }}
                    port_value: {{ .Values.rls.extAuthzPort | int }}

      - name: rls_ratelimit
        connect_timeout: 0.25s
        type: STRICT_DNS
        lb_policy: ROUND_ROBIN
        # 🔧 FIX: Properly configure HTTP/2 for gRPC communication to RLS
        typed_extension_protocol_options:
          envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
            "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
            explicit_http_config:
              http2_protocol_options:
                # 🔧 FIX: Configure HTTP/2 settings for better gRPC performance
                max_concurrent_streams: 100
        # 🔧 FIX: Add health checks for RLS rate limit service
        health_checks:
        - timeout: 1s
          interval: 5s
          unhealthy_threshold: 2
          healthy_threshold: 1
          http_health_check:
            path: "/health"
        # 🔧 FIX: Configure connection pool for better reliability
        circuit_breakers:
          thresholds:
          - priority: DEFAULT
            max_connections: 50
            max_pending_requests: 25
            max_requests: 100
            max_retries: 3
        load_assignment:
          cluster_name: rls_ratelimit
          endpoints:
          - lb_endpoints:
            - endpoint:
                address:
                  socket_address:
                    address: {{ .Values.rls.host }}
                    port_value: {{ .Values.rls.rateLimitPort | int }}
{{- end }} 